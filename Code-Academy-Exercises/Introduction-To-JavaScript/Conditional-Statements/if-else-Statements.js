let sale = true;

sale = false;

if(sale) {
  console.log('Time to buy!');
} else {console.log('Time to wait for a sale.')}


I learned to specify an else scenario which will run if the if condition is false
